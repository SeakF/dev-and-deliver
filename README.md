## to build app with docker run these commands in order

docker build -t dev-and-deliver .

docker-compose up -d


## to acces documentation check this url after starting app

http://localhost:3000/graphql